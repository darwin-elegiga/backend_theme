# Configuración del Backend de Temas
# Copiar a .env y ajustar según el entorno

# URL base para recursos estáticos
# Desarrollo: URL del backend local
# Producción: URL del CDN (ej: https://cdn.tudominio.com)
STATIC_BASE_URL=http://localhost:8000/static

# Orígenes CORS permitidos (separados por coma)
# Desarrollo: * (todos) o localhost
# Producción: dominios específicos del frontend
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# Cache en memoria
ENABLE_CACHE=true
CACHE_TTL=3600

# Ruta al archivo de configuración de brands (opcional)
# BRANDS_CONFIG_PATH=/app/config/brands.json

# Directorio de archivos estáticos (opcional)
# STATIC_DIR=/app/static
